{
  "name": "generate-interactive-business-report-dev",
  "private": true,
  "version": "1.0.0",
  "description": "Development-only tooling for the vendored interactive report runtime.",
  "scripts": {
    "build:charts": "esbuild scripts/echarts-entry.js --bundle --minify --format=iife --target=es2020 --legal-comments=inline --outfile=vendor/echarts.custom.min.js",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.63.0",
    "echarts": "6.1.0",
    "esbuild": "0.25.9"
  }
}
